use_default_settings: true

general:
  instance_name: "Local Agentic Search"
  debug: false

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  bind_address: "0.0.0.0"
  secret_key: "local-agentic-search-dev-secret"
  limiter: false
  image_proxy: false

ui:
  static_use_hash: true
